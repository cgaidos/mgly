import Map from 'collections/map'

/**
 *
 */
export class CategoryPopover {
    constructor ($button, $values = new Map) {
        this.button = $button;
        this.values = $values;
    }

    _template () {

    }
}
